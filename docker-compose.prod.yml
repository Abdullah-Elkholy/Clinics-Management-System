# =============================================================================
# Docker Compose - Production Environment (SECURITY HARDENED)
# =============================================================================
# Usage: docker-compose -f docker-compose.prod.yml up -d
# Secure PostgreSQL-based setup for production deployment

services:
  # PostgreSQL Database
  db:
    image: postgres:15-alpine
    container_name: clinics-db-prod
    environment:
      POSTGRES_USER: ${DB_USER:-clinics_user}
      POSTGRES_PASSWORD: ${DB_PASSWORD:?Database password required}
      POSTGRES_DB: ${DB_NAME:-ClinicsDb}
    volumes:
      - postgres_data_prod:/var/lib/postgresql/data
    healthcheck:
      test: [ "CMD-SHELL", "pg_isready -U ${DB_USER:-clinics_user} -d ${DB_NAME:-ClinicsDb}" ]
      interval: 10s
      timeout: 5s
      retries: 5
    restart: always
    # Security: No port exposure - accessed only via internal network
    # Security: Read-only root filesystem where possible
    security_opt:
      - no-new-privileges:true
    networks:
      - internal

  # API Service
  api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: clinics-api-prod
    environment:
      - ASPNETCORE_ENVIRONMENT=Production
      - DATABASE_PROVIDER=PostgreSQL
      - ConnectionStrings__DefaultConnection=Host=db;Database=${DB_NAME:-ClinicsDb};Username=${DB_USER:-clinics_user};Password=${DB_PASSWORD}
      - Jwt__Key=${JWT_SECRET_KEY:?JWT secret key required}
      - AllowedOrigins=${ALLOWED_ORIGINS}
      - ExtensionOrigins=${EXTENSION_ORIGIN}
    volumes:
      - api_logs:/app/logs
    expose:
      - "80"
    depends_on:
      db:
        condition: service_healthy
    restart: always
    security_opt:
      - no-new-privileges:true
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
    networks:
      - internal
      - frontend

  pgadmin:
    image: dpage/pgadmin4
    container_name: clinics-pgadmin
    environment:
      PGADMIN_DEFAULT_EMAIL: ${SSL_EMAIL:?SSL email required}
      PGADMIN_DEFAULT_PASSWORD: ${DB_PASSWORD}
      PGADMIN_CONFIG_ENHANCED_COOKIE_PROTECTION: "True"
      PGADMIN_CONFIG_LOGIN_BANNER: "'Authorized access only'"
      PGADMIN_CONFIG_CONSOLE_LOG_LEVEL: "30"
    # Security: Bind to localhost ONLY. Accessible via SSH Tunnel.
    ports:
      - "127.0.0.1:5050:80"
    depends_on:
      - db
    restart: always
    security_opt:
      - no-new-privileges:true
    networks:
      - internal

  # Web Frontend
  web:
    build:
      context: ./apps/web
      dockerfile: Dockerfile
      args:
        NEXT_PUBLIC_API_URL: ${API_URL}
    container_name: clinics-web-prod
    environment:
      - NEXT_PUBLIC_API_URL=${API_URL:-http://api}
      - NODE_ENV=production
    expose:
      - "3000"
    depends_on:
      - api
    restart: always
    security_opt:
      - no-new-privileges:true
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
    networks:
      - frontend

  # Nginx Reverse Proxy
  nginx:
    image: nginx:alpine
    container_name: clinics-nginx
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
      - ./nginx/ssl:/etc/nginx/ssl:ro
      - certbot_data:/var/www/certbot:ro
    depends_on:
      - api
      - web
    restart: always
    security_opt:
      - no-new-privileges:true
    networks:
      - internal
      - frontend

  # Certbot for SSL (optional - run manually)
  certbot:
    image: certbot/certbot
    container_name: clinics-certbot
    volumes:
      - certbot_data:/var/www/certbot
      - ./nginx/ssl:/etc/letsencrypt
    command: certonly --webroot -w /var/www/certbot --email ${SSL_EMAIL} -d ${DOMAIN} -d www.${DOMAIN} --agree-tos --no-eff-email
    profiles:
      - ssl
    networks:
      - frontend

networks:
  internal:
    driver: bridge
    internal: true
  frontend:
    driver: bridge

volumes:
  postgres_data_prod:
    driver: local
  certbot_data:
    driver: local
  api_logs:
    driver: local
