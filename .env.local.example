# Example local environment variables for running the solution locally
# Copy this file to .env.local (or set env vars in your shell) and edit values as needed.

# SQL Server connection string used by the Backend API when USE_LOCAL_SQL=true
# Use the format the app expects (SQL Server). Do NOT commit credentials.
LOCAL_SQL_CONN="Server=YOUR_SERVER\\SQLINSTANCE;Database=ClinicsDb;User Id=sa;Password=YourStrongPassword;TrustServerCertificate=True;"

# Optional: Ports used by services
ASPNETCORE_URLS_API=http://localhost:5000
ASPNETCORE_URLS_WHATSAPP=http://localhost:5100

# Note: You can also use 'dotnet user-secrets' for storing secrets per dev machine.
